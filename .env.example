# ---- App Profile ----
SPRING_PROFILES_ACTIVE=dev

# ---- MySQL (LOCAL) ----
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=shopmypham
DB_USER=shop_user
DB_PASS=ABC1234

# JDBC URL (có thể để trống để app tự ghép từ host/port/name)
DB_URL=jdbc:mysql://${DB_HOST}:${DB_PORT}/${DB_NAME}?useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=utf8&serverTimezone=UTC

# ---- JWT ----
JWT_SECRET=change-me-please-32chars-minimum-xxxxxxxxxxxxxxxx
JWT_EXPIRES_MINUTES=120

# ---- Admin mặc định (chỉ DEV) ----
ADMIN_EMAIL=admin@local
ADMIN_PASSWORD=admin123
